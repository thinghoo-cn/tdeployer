version: "3.0"

services:
  - name: qms
    host: 122.9.153.115
    prefix: /home/svtter
    repos:
      - qms_backend
      - qms_frontend
    stages:
      - name: dev
        path: work
      - name: prd
        path: work
      - name: master
        path: work
  - name: mes
    host: 1414141
    prefix: /sdfsdffsdfd
    stages:
      - name: dev